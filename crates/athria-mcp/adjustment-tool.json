{
  "name": "get_plan_adjustment_review",
  "description": "Review the current plan against fresh profile, training, calendar, and wellness facts. Returns deterministic reasons, scope, hard overrides, data gaps, and freshness hashes without modifying the plan.",
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "trigger": {
        "type": "string",
        "enum": ["weekly_review", "profile_change", "user_request"]
      }
    },
    "required": ["trigger"],
    "additionalProperties": false
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
