{
  "name": "athria-training-planner",
  "version": "0.3.0",
  "purpose": "Create and revise reusable session templates and the single current Mesocycle through Athria's deterministic tools.",
  "appliesWhen": ["The user asks to create, revise, validate, or explain a training plan."],
  "doesNotApplyWhen": ["The request is for medical diagnosis, injury treatment, or unsupervised rehabilitation."],
  "requiredContext": ["AthleteProfile", "TrainingPreference", "TrainingStateSnapshot", "ExerciseDefinition"],
  "allowedTools": [
    "get_athlete_profile", "get_training_preferences", "get_training_state", "list_training_sessions",
    "get_training_summary", "get_current_plan", "list_session_templates", "get_session_template", "get_exercise_catalog", "get_training_taxonomy", "calculate_training_metrics",
    "estimate_1rm", "calculate_heart_rate_zones", "evaluate_progression", "find_exercise_candidates",
    "validate_workout", "validate_current_plan", "check_training_constraints", "preview_session_template_change",
    "explain_validation_result", "create_session_template", "update_session_template", "delete_session_template", "save_current_plan", "propose_profile_update"
  ],
  "workflow": ["read", "identify-gaps", "save-templates", "plan", "validate", "revise", "explain", "save-current-plan"],
  "outputSchema": "CurrentPlan",
  "approvalPoints": ["Before deleting a template", "Before choosing how to handle affected future planned sessions"],
  "safetyRules": [
    "Do not infer missing health or recovery data.",
    "Do not bypass blocker validation, revision checks, or explicit deletion authority.",
    "Do not provide medical diagnosis or injury treatment."
  ]
}
