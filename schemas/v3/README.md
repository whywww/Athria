# Athria Plan Schema v3 contracts

These versioned JSON Schema files preserve the public v3 contract. V3 plans use sparse natural-week schedules, open exercises, component domains, classified facts, and rule-level `UNKNOWN`; `mixed` is not part of the plan taxonomy.
